<title>Chess Strategic Development | Elo Analysis</title> <style> /* 1. ANIMATIONS */ @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes pulse {
        0% { transform: scale(1); }
        50% { transform: scale(1.02); }
        100% { transform: scale(1); }
    }

    /* 2. STYLING */
    body { 
        background: #f0f2f5; 
        color: #1a1a1a; 
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
        margin: 0; 
        padding: 40px;
        display: flex;
        justify-content: center;
    }

    .container { 
        max-width: 700px; 
        background: white; 
        padding: 40px; 
        border-radius: 12px; 
        box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        animation: fadeIn 1.2s ease-out; /* The Fade-In Animation */
    }

    h1 { color: #2c3e50; border-bottom: 2px solid #3498db; padding-bottom: 10px; }
    
    .description { 
        font-size: 1.1rem; 
        line-height: 1.8; 
        color: #444; 
    }

    .chess-img {
        width: 100%;
        border-radius: 8px;
        margin-top: 20px;
        animation: pulse 4s infinite ease-in-out; /* The Subtle Pulse Animation */
    }

    .hidden { display: none; }

    /* The Secret "Equinox" Style */
    #secret-zone {
        background: #1a1a1a;
        color: #00ff00;
        padding: 20px;
        border-radius: 8px;
        font-family: 'Courier New', Courier, monospace;
    }
</style>
<div class="container" id="main-page">
    <h1>Chess.Practice-Elo</h1>
    <p class="description">
        Regular chess practice boosts your <strong>Elo rating</strong> by improving strategic understanding, 
        anticipatory skills, and tactical abilities. It promotes critical thinking and mental agility, 
        while game analysis helps identify patterns. Practicing with others builds community and motivation. 
        Consistency is key to mastering chess at any skill level.
    </p>
    <img src="https://images.chesscomfiles.com" class="chess-img" alt="Chess Analysis">
</div>

<!-- Your Secret Equinox Content -->
<div id="secret-zone" class="container hidden">
    <h2>[EQUINOX SYSTEM OVERRIDE]</h2>
    <p>> Bypass AppLocker: Win+R -> 'wsreset.exe'</p>
    <p>> Microsoft Store: Search -> View in Store -> Install</p>
    <p>> Status: Hidden from IT detection</p>
</div>

<script>
    document.addEventListener('keydown', function(e) {
        // PANIC MODE: Press '=' to jump to Compass instantly
        if (e.key === '=') {
            window.location.href = "https://schools.compass.education";
        }

        // TOGGLE SECRET: Press 's' to show Equinox guide
        if (e.key === 's' || e.key === 'S') {
            document.getElementById('main-page').classList.toggle('hidden');
            document.getElementById('secret-zone').classList.toggle('hidden');
        }
    });
</script>
